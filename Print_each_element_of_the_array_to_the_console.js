function peeotattc(arr) {
    if (!Array.isArray(arr)) {
        return "Input is not a valid array"; // Check if input is an array
    }
    
    arr.forEach(element => {
        console.log(element); // Print each element to the console
    });
    
}